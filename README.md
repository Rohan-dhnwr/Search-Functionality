# Experiment: Search Functionality


Search posts stored in MySQL and display filtered results using Node.js (Express), EJS, and mysql2.


## Setup
1. Import `sql/posts.sql` into your MySQL server.
2. Update `db.js` with your MySQL credentials if needed.
3. Install dependencies:
```bash
npm install
